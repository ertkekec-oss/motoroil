import { PrismaClient } from '@prisma/client'; const prisma = new PrismaClient(); async function run() { const res = await prisma.globalProduct.updateMany({ where: { status: 'DRAFT' }, data: { status: 'APPROVED' } }); console.log('Approved from DRAFT:', res.count); } run();
