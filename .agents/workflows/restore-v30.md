---
description: Terminal AI Features & Global Widget Inline Placement (5 Mart 2026)
---
// turbo-all
1. Reset all local changes
`git reset --hard`

2. Fetch all latest tags
`git fetch --all --tags`

3. Checkout to v30 milestone
`git checkout tags/v30`

4. Install dependencies
`npm install`

5. Verify running state
`npm run build`
